# Sample data

`hourly_prices_sample.csv` contains a tiny extract of hourly Old School RuneScape Grand Exchange price data for two items.

The full raw dataset is intentionally excluded from Git because it is generated data and is too large for normal source control. Use the data collection scripts to recreate the full dataset when needed.
